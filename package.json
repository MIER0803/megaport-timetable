{
  "name": "megaport-timetable",
  "private": true,
  "version": "1.0.0",
  "description": "大港開唱時刻表（Capacitor 包裝）",
  "scripts": {
    "prepare:www": "node scripts/copy-www.js",
    "build": "npm run prepare:www",
    "sync": "npm run prepare:www && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
